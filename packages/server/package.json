{
  "name": "autotoll-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot ./src/index.ts",
    "start": "bun ./src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun ./scripts/migrate.ts",
    "db:studio": "bun --bun drizzle-kit studio",
    "setup": "bun db:generate && bun db:migrate && bun ./scripts/seed-endpoints.ts"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "latest",
    "drizzle-orm": "latest",
    "hono": "latest",
    "nanoid": "latest",
    "x402-hono": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "latest",
    "postgres": "^3.4.7"
  }
}
